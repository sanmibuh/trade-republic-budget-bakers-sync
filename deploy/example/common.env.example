# Shared credentials — used by all services
# Copy to common.env and fill in real values

# Telegram notifications (optional — leave blank to disable)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# BudgetBakers Wallet API key (shared across all accounts)
WALLET_API_KEY=
